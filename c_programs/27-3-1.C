#include<stdio.h>
#include<conio.h>
void main()
{
char i ,x ,z;
      for (i='E';i>='A';i--)
      {
	 printf(" ");
	   for (z='A';z<=i;z++){
	    printf("%c",z);
	   }
	      for (x='A';x<=i;x++){

	      }

	  printf("\n");
       }
     getch();
    }
